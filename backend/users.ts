import { Router, } from "https://deno.land/x/oak@v10.4.0/router.ts";




export const userApiRoutes = (router: Router) => {

    router.post("/login", async () => {
    
        
    });

    router.post("/register", async () => {

    });
}